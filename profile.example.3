root sdc4 MyGentoo reiserfs
domainname easygentoo-test
hostname victim
rootpass ironman
username fiddler
userpass kido

# This profile means:
# Gentoo will be installed to /dev/sdc4
# CPU arch will be determined by "uname -m"
# Grub will be installed to /dev/sdc
# This will be a basic install (without Xfce and X, just console)
# We don't have a windows partition to add inside grub menu
# Easy Gentoo will try to handle connection problems.
# No limitations will be done to download speed
# Script will assume that we are installing on a desktop PC
